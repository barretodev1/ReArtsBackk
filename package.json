{
  "name": "reartsback",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "prisma generate && npm run build:app",
    "build:app": "echo 'No build command specified' && exit 1",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "prisma": "^5.19.1"
  }
}
